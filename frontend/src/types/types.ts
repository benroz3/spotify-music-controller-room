export interface SongType {
  title: string;
  artist: string;
  duration: number;
  time: number;
  image_url: string;
  is_playing: boolean;
  votes: number;
  votes_needed: number;
  id: string;
  preview_url: string;
}
